// Minimal type stubs for @actual-app/core/types/api-handlers
// Prevents TypeScript from resolving into raw .ts source files
export type ImportTransactionsOpts = {
  defaultCleared?: boolean;
  dryRun?: boolean;
  reimportDeleted?: boolean;
};
